package view;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.AbstractButton;
import javax.swing.JButton;
import javax.swing.JPanel;

public class ButtonExample extends JPanel implements ActionListener {
	/*
	 * protected JButton b1, b2, b3; public ButtonExample() { b1 = new
	 * JButton("Disable middle button");
	 * b1.setVerticalTextPosition(AbstractButton.CENTER);
	 * b1.setHorizontalTextPosition(AbstractButton.LEADING); //aka LEFT, for
	 * left-to-right locales b1.setActionCommand("disable"); b2 = new
	 * JButton("Middle button"); b2.setVerticalTextPosition(AbstractButton.BOTTOM);
	 * b2.setHorizontalTextPosition(AbstractButton.CENTER); b3 = new
	 * JButton("Enable middle button"); b3.setActionCommand("enable");
	 * b3.setEnabled(false); //Listen for actions on buttons 1 and 3.
	 * b1.addActionListener( this ); b3.addActionListener( this );
	 * add(b1);add(b2);add(b3);}
	 */
	
 public void actionPerformed(ActionEvent e) {
       /*   if ("disable".equals(e.getActionCommand())) {
            b2.setEnabled( false ); b1.setEnabled( false ); b3.setEnabled( true );
        } else {
            b2.setEnabled( true ); b1.setEnabled( true );b3.setEnabled( false );}*/
	 }}
